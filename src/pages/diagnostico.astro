---
// Página de diagnóstico para testear endpoints API
const siteUrl = import.meta.env.PUBLIC_SITE_URL || 'http://localhost:4321';
---

<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Diagnóstico API - FashionStore</title>
  <style>
    body {
      font-family: system-ui, -apple-system, sans-serif;
      max-width: 1200px;
      margin: 0 auto;
      padding: 20px;
      background: #0f0f0f;
      color: #e0e0e0;
    }
    .container {
      background: #1a1a1a;
      border-radius: 12px;
      padding: 30px;
      margin-bottom: 20px;
      border: 1px solid #333;
    }
    h1 {
      color: #c6ff00;
      margin-top: 0;
    }
    h2 {
      color: #c6ff00;
      border-bottom: 2px solid #333;
      padding-bottom: 10px;
    }
    button {
      background: #c6ff00;
      color: #000;
      border: none;
      padding: 12px 24px;
      border-radius: 6px;
      cursor: pointer;
      font-weight: 600;
      margin: 5px;
      font-size: 14px;
    }
    button:hover {
      background: #d4ff33;
    }
    button:disabled {
      background: #555;
      cursor: not-allowed;
    }
    .result {
      background: #0a0a0a;
      border: 1px solid #333;
      border-radius: 6px;
      padding: 15px;
      margin-top: 15px;
      font-family: 'Courier New', monospace;
      font-size: 13px;
      white-space: pre-wrap;
      word-break: break-all;
    }
    .success {
      border-left: 4px solid #4caf50;
    }
    .error {
      border-left: 4px solid #f44336;
    }
    .warning {
      border-left: 4px solid #ff9800;
    }
    .info {
      color: #90caf9;
    }
    .label {
      color: #888;
      font-size: 12px;
      margin-top: 10px;
      display: block;
    }
    input, textarea {
      width: 100%;
      padding: 10px;
      background: #0a0a0a;
      border: 1px solid #333;
      border-radius: 4px;
      color: #e0e0e0;
      margin-top: 5px;
      font-family: inherit;
    }
    .grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 20px;
    }
    @media (max-width: 768px) {
      .grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>🔧 Diagnóstico de API - FashionStore</h1>
    <p>Esta página te ayuda a diagnosticar problemas con los endpoints API.</p>
    <p class="info">Site URL: <strong>{siteUrl}</strong></p>
  </div>

  <div class="grid">
    <div class="container">
      <h2>1. Test Endpoint Básico</h2>
      <p>Verifica que los requests POST funcionan en general.</p>
      
      <button id="testGetBtn">🟢 Test GET /api/test</button>
      <button id="testPostBtn">🟠 Test POST /api/test</button>
      
      <div id="testResult"></div>
    </div>

    <div class="container">
      <h2>2. Test Login Endpoint</h2>
      <p>Prueba el endpoint de autenticación.</p>
      
      <label class="label">Email:</label>
      <input type="email" id="loginEmail" value="test@example.com" placeholder="tu@email.com">
      
      <label class="label">Password:</label>
      <input type="password" id="loginPassword" value="test123" placeholder="Tu contraseña">
      
      <button id="testLoginBtn" style="margin-top: 15px;">🔐 Test POST /api/auth/login</button>
      
      <div id="loginResult"></div>
    </div>
  </div>

  <div class="container">
    <h2>3. Información del Navegador</h2>
    <div class="result info">
      <strong>User Agent:</strong> <span id="userAgent"></span><br>
      <strong>URL actual:</strong> <span id="currentUrl"></span><br>
      <strong>Cookies habilitadas:</strong> <span id="cookiesEnabled"></span><br>
      <strong>Protocol:</strong> <span id="protocol"></span>
    </div>
  </div>

  <div class="container">
    <h2>📋 Guía de Interpretación</h2>
    
    <h3>✅ Si todos los tests pasan:</h3>
    <ul>
      <li>Los endpoints API funcionan correctamente</li>
      <li>El problema puede estar en las credenciales o en la BD</li>
      <li>Revisa los logs de Coolify para más detalles</li>
    </ul>

    <h3>❌ Si GET funciona pero POST falla con 403:</h3>
    <ul>
      <li><strong>Cloudflare WAF</strong> está bloqueando POST requests</li>
      <li>Solución: Desactiva proxy de Cloudflare temporalmente (DNS Only)</li>
      <li>O crea regla de whitelist en Cloudflare WAF</li>
    </ul>

    <h3>❌ Si ambos GET y POST fallan:</h3>
    <ul>
      <li>Problema de conectividad o DNS</li>
      <li>Verifica que el dominio resuelve correctamente</li>
      <li>Revisa estado del servidor en Coolify</li>
    </ul>
  </div>

  <script>
    // Display browser info
    document.getElementById('userAgent').textContent = navigator.userAgent;
    document.getElementById('currentUrl').textContent = window.location.href;
    document.getElementById('cookiesEnabled').textContent = navigator.cookieEnabled ? 'Sí ✓' : 'No ✗';
    document.getElementById('protocol').textContent = window.location.protocol;

    // Helper to display results
    function displayResult(elementId, data, isError = false) {
      const el = document.getElementById(elementId);
      const className = isError ? 'error' : 'success';
      el.innerHTML = `<div class="result ${className}">${JSON.stringify(data, null, 2)}</div>`;
    }

    // Test GET /api/test
    document.getElementById('testGetBtn').addEventListener('click', async () => {
      const btn = document.getElementById('testGetBtn');
      btn.disabled = true;
      btn.textContent = '⏳ Probando...';
      
      try {
        const start = Date.now();
        const response = await fetch('/api/test', {
          method: 'GET'
        });
        const duration = Date.now() - start;
        
        const data = await response.json();
        
        displayResult('testResult', {
          status: response.status,
          statusText: response.statusText,
          duration: `${duration}ms`,
          headers: {
            contentType: response.headers.get('content-type'),
            server: response.headers.get('server'),
            cfRay: response.headers.get('cf-ray')
          },
          data
        }, !response.ok);
        
      } catch (error) {
        displayResult('testResult', {
          error: error.message,
          type: error.constructor.name
        }, true);
      } finally {
        btn.disabled = false;
        btn.textContent = '🟢 Test GET /api/test';
      }
    });

    // Test POST /api/test
    document.getElementById('testPostBtn').addEventListener('click', async () => {
      const btn = document.getElementById('testPostBtn');
      btn.disabled = true;
      btn.textContent = '⏳ Probando...';
      
      try {
        const start = Date.now();
        const formData = new FormData();
        formData.append('test', 'hello');
        formData.append('timestamp', new Date().toISOString());
        
        const response = await fetch('/api/test', {
          method: 'POST',
          body: formData
        });
        const duration = Date.now() - start;
        
        const text = await response.text();
        let data;
        try {
          data = JSON.parse(text);
        } catch {
          data = { rawResponse: text };
        }
        
        displayResult('testResult', {
          status: response.status,
          statusText: response.statusText,
          duration: `${duration}ms`,
          headers: {
            contentType: response.headers.get('content-type'),
            server: response.headers.get('server'),
            cfRay: response.headers.get('cf-ray')
          },
          data
        }, !response.ok);
        
      } catch (error) {
        displayResult('testResult', {
          error: error.message,
          type: error.constructor.name,
          stack: error.stack
        }, true);
      } finally {
        btn.disabled = false;
        btn.textContent = '🟠 Test POST /api/test';
      }
    });

    // Test Login
    document.getElementById('testLoginBtn').addEventListener('click', async () => {
      const btn = document.getElementById('testLoginBtn');
      const email = document.getElementById('loginEmail').value;
      const password = document.getElementById('loginPassword').value;
      
      btn.disabled = true;
      btn.textContent = '⏳ Probando login...';
      
      try {
        const start = Date.now();
        const formData = new FormData();
        formData.append('email', email);
        formData.append('password', password);
        formData.append('redirectTo', '/cuenta');
        
        const response = await fetch('/api/auth/login', {
          method: 'POST',
          body: formData
        });
        const duration = Date.now() - start;
        
        const text = await response.text();
        let data;
        try {
          data = JSON.parse(text);
        } catch {
          data = { rawResponse: text.substring(0, 500) };
        }
        
        displayResult('loginResult', {
          status: response.status,
          statusText: response.statusText,
          duration: `${duration}ms`,
          headers: {
            contentType: response.headers.get('content-type'),
            server: response.headers.get('server'),
            cfRay: response.headers.get('cf-ray'),
            setCookie: response.headers.get('set-cookie') ? 'Present' : 'None'
          },
          data
        }, !response.ok);
        
      } catch (error) {
        displayResult('loginResult', {
          error: error.message,
          type: error.constructor.name,
          stack: error.stack
        }, true);
      } finally {
        btn.disabled = false;
        btn.textContent = '🔐 Test POST /api/auth/login';
      }
    });
  </script>
</body>
</html>
